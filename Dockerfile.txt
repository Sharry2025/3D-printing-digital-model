# 使用 Python 3.9 作为基础镜像
FROM python:3.9

# 设置工作目录（容器内的路径）
WORKDIR /app

# 复制本地文件到容器
COPY . .

# 安装依赖（requirements.txt 中的库）
RUN pip install --no-cache-dir -r requirements.txt

# 启动命令（运行 Streamlit）
CMD ["streamlit", "run", "app.py", "--server.port=8080", "--server.address=0.0.0.0"]